export const metadata = {
  title: "Book stays & plan your trip | TravelTeasing",
  description: "Select dates, travelers, budget and destination to find and book stays for your journey.",
};

export default function BookingLayout({ children }) {
  return children;
}
