export const metadata = {
  title: "Search temples, treks & places | TravelTeasing",
  description: "Search across temples, treks, and off-beat destinations. Find your next trip by place, state, or theme.",
};

export default function SearchLayout({ children }) {
  return children;
}
