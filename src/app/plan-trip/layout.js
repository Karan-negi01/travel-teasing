export const metadata = {
  title: "Plan Your Trip | TravelTeasing",
  description:
    "Build your journey — choose temples, treks, and off-beat stays. Book transport and hotels in one place.",
};

export default function PlanTripLayout({ children }) {
  return children;
}
